# pgrep

* use the boiler plate software
* first run pip install -r requirements

## Final Goal
    * Enter a single commmand from the command line or a list of commands 
    * result should printed back on screen with the line number found as well
    * optinally, write results back to a file
    * Nice to haves
     
    
## First challege:

Create in manager.py a new command that prints two given arguments 

## Second challenge

Open the twilio.json file a search for string "account_sid"

## Challenge 3: 

Count the number of times you found the string

# Hints

* __use the boilerplate code__
* to run a command in manager.py go the boilerplate directory
   ```bash
    python3 manager.py find_in_file rene
 
```
